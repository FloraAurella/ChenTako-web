Settings · 设置
设置
本地服务正常
偏好设置
外观
模型与供应商
上下文与提示词
工具
技能
数据管理
关于与更新
上下文与提示词
当前作用范围
选择配置范围
聊天默认配置
研究项目
所有普通聊天
项目未自定义时跟随更新
恢复应用默认
常用配置
系统提示词

下次发送生效，切换模型时保留。

提示词内容
0 / 102,400 字符
展开编辑器
清空提示词
上下文策略
输入预算（Tokens）

留空 = 模型窗口 − 最大输出 − 5% 安全余量。

自动压缩历史

达阈值后额外请求模型压缩历史，保留聊天记录。

压缩触发阈值（%）

达到输入预算的此比例时压缩。

demo-model 的可用输入预算：116,326 Tokens（估算）。 按当前模型发送，用量为估算。

高级配置
生成参数

以模型和 API 支持为准。

默认思考强度
轻量
中等
高
极高
最大

可在聊天中临时调整。

Temperature

随机性；部分推理模型不支持。

Top P

采样范围；Gemini 思考模式不使用。

输出与会话
流式输出

边生成边显示。

本地保存对话

仅影响新会话。

User ID

可选；Gemini 不使用。

模型兼容性

手动修正图片能力，对所有项目生效。

选择模型
示例供应商 / demo-model
图片输入能力
自动识别
支持
关闭
图片输出能力
自动识别
支持
关闭
配置已保存
放弃更改
保存更改
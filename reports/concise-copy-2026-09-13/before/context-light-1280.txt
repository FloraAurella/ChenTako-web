Settings · 设置
设置
本地服务正常
偏好设置
外观
模型与供应商
上下文与提示词
工具
技能
数据管理
关于与更新
上下文与提示词

统一设置聊天行为，让每个项目保留需要的差异。

当前作用范围
选择配置范围
聊天默认配置
研究项目
所有普通聊天
项目未覆盖的设置也会跟随更新
恢复应用默认
常用配置

先确定助手指令与上下文策略。项目未覆盖的字段持续跟随聊天默认配置。

系统提示词

从下一次发送起生效，切换模型时保留。

提示词内容

技能和必要的工具运行指令由应用单独添加。

0 / 102,400 字符
展开编辑器
清空提示词
上下文策略

控制发送多少上下文，保留模型窗口与输出额度的安全余量。

输入预算（Tokens）

留空自动计算：模型窗口 − 最大输出 − 5% 窗口安全余量。

自动压缩历史

达到阈值时压缩已完成的历史，不按固定轮数保留；会产生额外模型请求，聊天记录不会删除。

压缩触发阈值（%）

占用达到输入预算的此比例时，在发送前压缩一次。

demo-model 的可用输入预算：116,326 Tokens（估算）。 实际发送使用当前会话模型；附件与工具内容采用保守估算。

高级配置

按模型需要调整生成、输出和兼容性参数；无需修改时可保留默认值。

生成参数

实际适用参数由模型与 API 协议决定。

默认思考强度
轻量
中等
高
极高
最大

聊天工具栏可临时覆盖，也可恢复跟随配置。

Temperature

控制采样随机性；部分推理模型不接受此参数。

Top P

控制采样候选范围；Gemini 思考模式不发送采样参数。

输出与会话
流式输出

边生成边显示；关闭后等待完整响应。

本地保存对话

用于新建会话；已有会话保留原保存状态。

User ID

可选；Gemini 不发送此字段，其他协议按各自格式传递。

模型兼容性

全局配置，不参与项目继承。用于纠正模型图片能力的自动识别。

选择模型
示例供应商 / demo-model
图片输入能力
自动识别
支持
关闭
图片输出能力
自动识别
支持
关闭
配置已保存
放弃更改
保存更改
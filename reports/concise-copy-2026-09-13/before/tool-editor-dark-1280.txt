Settings · 设置
设置
本地服务正常
偏好设置
外观
模型与供应商
上下文与提示词
工具
技能
数据管理
关于与更新
新增工具

名称会作为模型函数名，输入 Schema 必须是 object。

工具配置
工具名称

以字母开头，可使用字母、数字、_ 和 -。

用途说明
HTTP Endpoint

使用 POST JSON 调用，仅支持 HTTPS 或本机回环 HTTP。

输入 Schema（JSON）

根节点必须是 { "type": "object" }，最多 16KB。

全局启用
启用后保留该配置；本版本不会实际调用。
保存后保留配置；工具执行待后续版本开放
取消
保存工具
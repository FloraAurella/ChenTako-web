## Review report: 章节指令与右侧栏

**Overall score: 100/100 — Ship**
*Correctness of assessment: High (已验证本地逻辑与浏览器交互；本轮不涉及真实模型效果验证)*

### Blockers (must fix)
None.

### Warnings (should fix)
None.

### Suggestions (optional)
None.

### Verdict
Ship。创建／重命名仅保留指令入口；右侧栏图标通过公共插槽注册，开合不卸载编辑内容。

## 验证记录

- 前端与后端架构检查通过；前后端 TypeScript、生产构建通过。
- 全量 Vitest：60 文件、504 / 504 通过，见 unit-tests.log。
- 写作 Playwright：20 / 20 通过（模拟响应），覆盖模式、全文、正文版本、取消、切换会话、备份、预算、存储失败、指令重命名、反向动画、减少动态效果与编辑保留。
- 写作布局复验：6 / 6 通过，浅深主题 × 1280／1024／390。截图等待位移和透明度过渡完成后生成，已人工查看桌面浅色和手机深色截图。
- 指令键盘／输入法／菜单专项：9 / 9 通过。
- 指令面板布局：桌面 4 项通过，手机 2 项初次因硬编码 7 次方向键失败；新增 /name 使菜单从 8 项变为 9 项，改为按实际数量遍历，保留最后一项选择与滚动断言后复验 2 / 2 通过。
- 初次写作回归发现 composer 的已有 width:100% 与 margin 布局冲突，侧栏遮住发送按钮；修正为明确剩余宽度后 20 项通过。
- 首次全量单元检查发现 jsdom 无 matchMedia；媒体查询加入能力检测后 504 项通过。
- 没有删除或跳过测试，没有读取或修改作者提示词正文，没有调用真实模型。
- 本轮仅跑相关浏览器回归，不宣称历史全站浏览器基线全部通过。

## 实现边界

/name 使用双参数解析，支持中文、双引号空格和转义引号；通过领域 rename 保留章节 ID、正文、版本、模式，集中拒绝重复名称、缺失章节、编辑锁和生成锁。
chat.header.actions 是通用插槽，writing 贡献按钮，App 不判断写作业务。右侧栏以 transform/opacity 与 240ms 共享令牌开合，快速反向时 CSS 从当前值继续；减少动态效果取消位移。关闭状态 inert + aria-hidden；手机复用同一图标切换视图。桌面折叠随作品保存，手机视图维持既有运行时行为。
